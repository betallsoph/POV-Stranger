// Copy to Secrets.xcconfig and fill in after creating Atlas App Services endpoints.
// Add Secrets.xcconfig to Xcode project build configurations (Debug/Release).
// This file is safe to commit — real secrets stay in Secrets.xcconfig (gitignored).

ATLAS_ENDPOINT_BASE =
ATLAS_APP_ID =

# Sign in with Apple: enable capability for bundle id antt.POV-Stranger
# in Apple Developer + Xcode, then enable Apple provider in Atlas Auth.
